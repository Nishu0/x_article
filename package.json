{
  "name": "x-article-progress",
  "version": "1.0.0",
  "description": "Track reading progress on X long-form articles",
  "scripts": {
    "build": "bun run build.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.0"
  }
}
